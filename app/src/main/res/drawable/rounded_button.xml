<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="20dp"/> <!-- ปรับความโค้งของมุม -->
    <solid android:color="@color/white"/> <!-- สีพื้นหลัง -->
    <stroke android:width="2dp" android:color="@color/black"/> <!-- เส้นขอบสีดำ -->
</shape>
